<h1>All Landmarks</h1>


  <% if @landmarks.empty? %>
  There are no landmarks saved yet!
  <% else %>
  <ul>
  <% @landmarks.each do |l| %>
  <li><%= l.name %>, <%=l.year_completed%></li>
  <% end %>
  </ul>

<%end%>
