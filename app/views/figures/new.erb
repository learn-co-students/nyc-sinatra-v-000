<h1>Use this form to make a new Figure!</h1>

<form action="/figures" method="post">

  <p>Figure's Name: <input type="text" name="figure[name]" id="figure_name"></p>

  <h4>Choose this Figure's titles:</h4>
  <% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][<%= title.id %>]" id="title_<%= title.id %>"> <%= title.name %> </input>
    <br>
  <% end %>
  <p>-or- provide your own title: <input type="text" name="title[name]" id="new_title"></p>

  <h4>Choose the landmarks associated with this Figure:</h4>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" id="title_<%= landmark.id %>"> <%= landmark.name %> </input>
    <br>
  <% end %>
  <p>-or- provide your own landmark: <input type="text" name="landmark[name]" id="new_landmark"></p>

  <input type="submit" name="submit" value="Create New Figure" id="Create New Figure">
</form>
