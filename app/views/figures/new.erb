<h1>Create A Figure</h1>

<form action="/figures" method="post">

  <p><h4>Figure: </h4><input type="text" name="figure[name]" id="figure_name"></p>

  <p>Choose a landmark and title:</p>

<h4>Landmarks:</h4>
  <p><% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>", id="landmark_<%= landmark.id %>"><%= landmark.name %></input>
  <% end %></p>

<h4>Titles:</h4>
  <p><% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>", id="title_<%= title.id %>"><%= title.name %></input>
  <% end %></p>

  <br></br>
  <p>Add a new landmark and/or title:</p>

  <p><label>New Landmark Name: </label><input type="text" name="landmark[name]" id="new_landmark"> and Year Completed: <input type="text" name="landmark[year_completed]"></p>

  <p><label>New Title:</label><input type="text" name="title[name]" id="new_title"></p>

  <input type="submit" value="Create New Figure" id="submit">
</form>
