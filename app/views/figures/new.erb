<h1>Create a New Figure</h1>
<form action="/figures" method="post">
  <h3>
    <label for="Name">Name: <input type='text' id='figure_name' name='figure[name]'></label>
  </h3>
  <br></br>
  <h3>
    <label for="Titles">Select some o' them der titles:
      <br>
      <% @titles.each do |title|%>
      <%= title.name %>
      <input type="checkbox" name="figure[title_ids][]" id='title_<%=title.id%>' value="<%=title.id%>">
      <% end %>
    </label>
  </h3>
  <br></br>
  <h3>
    Or, create one!
    <input type="text" name="title[name]" id="new_title">
  </h3>
  <br></br>
  <h3>
    <label for="landmarks">Select some o' dem der landmarks, mkay:
      <br>
      <% @landmarks.each do |landmark| %>
      <%= landmark.name %>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id='landmark_<%=landmark.id%>'>
      <% end %>
    </label>
  </h3>
  <br></br>
  <h3>
    Or, create one! Or, DON'T.
    <label for="new_landmark_name">
      Landmark Name:
      <input type="text" name="landmark[name]" id='new_landmark'>
    </label>
    <label for="new_landmark_year">
      Year Completed:
      <input type="text" name="landmark[year_completed]" id='year_completed'>
    </label>
  </h3>
    <input type="submit" value="Create New Figure">
</form>
