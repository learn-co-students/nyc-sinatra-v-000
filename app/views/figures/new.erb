<h1>Enter a New Figure!</h1>
<h2>Fill out the form below:</h2>

<br>
<form action="/figures" method="post">
  <p>Figure Name: <input type="text" name="figure[name]" id="figure_name"></p>
  <br>

  <h2>Select title(s) below:</h2>
    <% @titles.each do |t| %>
      <input type="checkbox" name="figure[title_ids][]" id="title_<%=t.id%>" value="<%=t.id%>"><%=t.name%></input>
    <% end %>
  <br>
  <h3>And/or create a new title: <input type="text" name="title[name]" id="new_title"></h3>
  <br>

  <h2>Select landmark(s) below:</h2>
    <% @landmarks.each do |lm| %>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%=lm.id%>" id="landmark_<%=lm.id%>"><%=lm.name%></input>
    <% end %>
  <br>
  <h3>And/or create a new landmark:</h3>
  <p>Name: <input type="text" name="landmark[name]" id="new_landmark"> Year: <input type="text" name="landmark[year_completed]" id="new_landmark_year"></p>
  <br>

  <input type="submit" id="Create New Figure" name="Create New Figure" value="Create New Figure">
</form>
