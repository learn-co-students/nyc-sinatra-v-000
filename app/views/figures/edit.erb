<h1>Edit information for <%= @figure.name %>:</h1>
<form action="/figures/<%= @figure.id %>" method="POST">
  <input type="hidden" id="hidden" name="_method" value="PATCH">
  <label for="figure_name">Name:</label>
  <input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>">
  <br><br>
  <h3><label>Titles</label></h3>
  <% @titles.each do |title| %>
    <input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %>><%= title.name %></input>
    <br>
  <% end %>
  <br>
  <h3><label>Create New Title:</label></h3>
  <input type="text" id="new_title" name="title[name]">
  <br><br>
  <h3><label>Landmarks</label></h3>
  <% @landmarks.each do |landmark| %>
    <input type="checkbox" id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>><%= landmark.name %></input>
    <br>
  <% end %>
  <br>
  <h3><label>Create New Landmark:</label></h3>
  <label for="new_landmark">Name:</label>
  <input type="text" id="new_landmark" name="landmark[name]">
  <label for="landmark_year_completed">Year:</label>
  <input type="text" id="landmark_year_completed" name="landmark[year_completed]">
  <br><br>
  <button type="submit" value="create new figure">Edit Figure</button>
</form>
