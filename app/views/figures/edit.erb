

<h1>Edit: <%=@figure.name%></h1>

<form method="POST" action="/songs/<%= @figure.id %>">
  <input type="hidden" name="_method" value="PATCH" id="hidden">
  
  <p>
  <label>Name: </label>
  <input type="text" name="figure[name]" id="figure_name" value="<%=@figure.name %>">
  </p>
  
  <label>Title:</label>
  <% Title.all.each do |title| %>
    <p><input type="checkbox" name="figure[titles][]" id="<%= title.name %>"  value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>><%= title.name %></p>
  <% end %>
  <br>
  
   <p><label>New Title</label>
     <input type="text" name="title[name]" id="new_title"></p>
  
  <label>Landmark:</label>
  <% Landmark.all.each do |landmark| %>
    <p><input type="checkbox" name="figure[landmarks][]" id="<%=landmark.name%>"  value="<%=landmark.id%>" <%='checked' if @figure.landmarks.include?(landmark) %>><%= landmark.name %></p>
  <% end %>
  
  <p><label>New Landmark</label><br>
  <input type="text" name="landmark[name]" id="new_title" value="name"></p>
  <input type="text" name="landmark[name]" id="new_title" value="year completed"></p>
  <br>
    <input type="submit" value="Update Figure">
    
</form>


