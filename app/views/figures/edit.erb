<h1>Edit Figure</h1>

<form action="/figures/<%= @figure.id %>" method="post">
  <input type="hidden" name="_method" value="PATCH">
  <label>Figure Name: <input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>">

  <br></br>
  <br></br>

  <h4>Edit Titles:</h4>

  <% Title.all.each do |title|%>
    <%=title.name%><input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %>></input></br>
    <%end%>

  <h4>and/or, create a new title:</h4>
      <input type="text" id="new_title" name="title[name]">

  <br></br>
  <br></br>

<h4>Edit Landmarks:</h4>

<% Landmark.all.each do |landmark| %>
    <%= landmark.name%>
      <input type="checkbox" id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>></input><br>
    <%end%>

  <h4>and/or, create a new landmark:</h4>
  <label>Name:</label>
      <input type="text" name="landmark[name]" id="new_landmark">
  <label>Year:</label>
      <input type="text" name="landmark[year_completed]" id="landmark_year">

  <br></br>

  <input type="submit" value="Edit Figure">


</form>
