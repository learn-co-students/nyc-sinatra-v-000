<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Figure</title>
    <h1>Edit <%= @figure.name %></h1>
  </head>
  <body>
    <form action="/figures/<%= @figure.id %>" method="post">
      <label>Name:</label>
      <input type="text" name="figure[name]" value="<%= @figure.name %>"
      id="figure_name">
      <br></br>
      <label>Change title(s):</label>
      <% Title.all.each do |title| %>
        <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>"
        id="<%= title.id %>"<%='checked' if @figure.titles.include?(title)%>>
        <%= title.name %></input>
        <br>
      <% end %>
      <br></br>
      <label>Create new title:</label>
      <input type="text" name="title[name]" id="new_title">
      <br></br>
      <label>Change landmark(s):</label>
      <% Landmark.all.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>"
        id="<%=landmark.id%>"<%='checked' if @figure.landmarks.include?(landmark)%>>
        <%= landmark.name %></input>
      <% end %>
      <br></br>
      <label>Create new landmark:</label>
      <p><label>Name: </label><input type="text" name="landmark[name]"
        id="new_landmark">
      <label>Year: <input type="text" name="landmark[year_completed]"
        id="new_landmark_year"></label></p>
      <br>
      <input type="submit" value="Edit Figure">
    </form>
  </body>
</html>
