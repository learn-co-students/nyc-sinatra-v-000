<!DOCTYPE html>
<html>
<head>
	<title>Edit a Figure</title>
</head>
<body>
	<form action="/figures/<%=@new_figure.id%>" method="post">

	<input type="text" name="figure[name]" id="figure_name" value="<%=@new_figure.name%>"> </input>

	<br></br>

	<!--Choose an from existing titles-->
	<%Title.all.each do |title| %>
	<label for="figure[title_ids][]">Titles</label>
	<input type="checkbox" name="figure[title_ids][]" id="<%=title.id%>" value="<%=title.id%>" <%='checked' if @new_figure.titles.include?(title) %>><%=title.name%></input>
	<%end%>

	<br></br>

	<!--Or create a new title-->
		<label for="title[name]">New Title Name</label>
		<input type="text" name="title[name]" id="new_title"></input>

		<br></br>

		


		<!--Choose an from existing Landmarks-->
		<%Landmark.all.each do |landmark| %>
		<label for="figure[landmark_ids][]">Landmarks</label>
		<input type="checkbox" name="figure[landmark_ids][]" id="<%=landmark.id%>" value="<%=landmark.id%>" <%='checked' if @new_figure.landmarks.include?(landmark) %>><%=landmark.name%></input>
		<%end%>
		<br></br>


		<!--Or create a new landmark-->
		<label for="landmark[name]">New Landmark</label>
		<input type="text" name="landmark[name]" id="new_landmark"></input>
		<input type="text" name="landmark[year_completed]" id="year_completed"></input>
	
		<input type="submit" value="Edit Figure">


		</form>


</body>
</html>
