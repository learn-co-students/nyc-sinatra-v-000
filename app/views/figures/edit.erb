<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Edit figure</title>
  </head>
  <body>
    <h1>Edit <%=@figure.name%></h1>
    <form class="form" action="/figures/<%=@figure.id%>" method="post">
      <input type="hidden" name="_method" value="patch">
      <input type="text" id="figure_name" name="figure[name]" value="Figure name">
      <br></br>
      Titles:
      <%@titles.each do |title|%>
        <li><input type="checkbox" id="title_<%=title.id%>" name="figure[title_ids][]" value="<%=title.id%>" <%='checked' if @figure.titles.include?(title)%>> <%=title.name%></li>
      <%end%>
      Or Create new Title: <input type="text" id="new_title" name="title[name]" placeholder="New title name">
      <br></br>
      Landmark:
      <%@landmarks.each do |landmark|%>
        <li><input type="checkbox" id="landmark_<%=landmark.id%>" name="figure[landmark_ids][]" value="<%=landmark.id%>"<%='checked' if @figure.landmarks.include?(landmark)%>> <%=landmark.name%></li>
      <%end%>
      Or Create new Landmark: <input type="text" id="new_landmark" name="landmark[name]" placeholder="New landmark name">
      <input type="text" id="new_landmark_year_completed" name="landmark[year_completed]" placeholder="New landmark year of completion">
      <br></br>
      <br></br>
      <input type="submit" value="Edit Figure">
    </form>
  </body>
</html>
