<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Editing <%= @figure.name %></title>
  </head>
  <body>
    <h1>Editing Figure</h1>

    <form class="" action="/figures/<%= @figure.id %>" method="post">
      <h4>Name: <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>"></h4>

      <h4>Titles:</h4>
      <% Title.all.each do |title| %>
        <p><input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>"><%= title.name %></p>
      <% end %>

      <h4>Create a new title</h4>
        <p>Name: <input type="text" name="title[name]"></p>

      <h4>Landmarks:</h4>
      <% Landmark.all.each do |landmark| %>
        <p><input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>"><%= landmark.name %></p>
      <% end %>

      <h4>Create a new landmark</h4>
        <p>Name: <input type="text" name="landmark[name]" id="new_landmark"></p>
        <p>Year Completed: <input type="text" name="landmark[year_completed]" id="new_landmark_year"></p>

      <br><br>
      <input type="submit" value="Edit Figure">
    </form>
  </body>
</html>
